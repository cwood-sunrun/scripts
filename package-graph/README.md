## Package Graph 

Given a package-lock.json file, output an html file that visualizes the dependency graph of the project. 

```
package-graph package-lock.json
```

### Compile

Requires [go](https://go.dev/). 

```
go build main.go -o package-graph
```

